#include "push_swap.h"


int	main(int argc, char **argv)
{
	char	*a;
	char	*b;
	if (check_error(argv))
		return (1)
	a = malloc(sizeof(char)*(sizep));
	b = malloc(sizeof(char)*(sizep));
	a = init(argv, argc);
	
}
